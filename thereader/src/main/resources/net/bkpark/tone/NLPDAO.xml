<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper      
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"      
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.bkpark.tone.NLPDAO">
	<insert id="insertEmail" parameterType="EmailVO">
		insert into tbl_email(sender, date, subject, text)
		values(#{sender},#{date},#{subject},#{text})
	</insert>
	
	<select id="getList" resultType="EmailVO">
		select no, sender, date, subject, text from tbl_email	
	</select>
	
	
	<select id="getDetail" resultType="EmailVO" parameterType="int">
		select no, sender, date, subject, text 
		from tbl_email
		where no=#{no}	
	</select>
</mapper>